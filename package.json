{
  "name": "cafe-monorepo",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "setup:local": "cd docker && docker compose up -d && npx supabase start && cd ../packages/db && npx prisma db push",
    "lint": "turbo lint",
    "format": "turbo format",
    "test": "turbo test",
    "clean": "turbo clean && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "5.0.5",
    "turbo": "2.9.18"
  },
  "packageManager": "pnpm@11.6.0",
  "engines": {
    "node": ">=24"
  }
}
