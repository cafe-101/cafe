services:
  redis:
    image: redis:8.8.0-alpine
    ports:
      - "${REDIS_PORT:-4045}:6379"
    volumes:
      - redisdata:/data
    networks:
      - cafe-net

volumes:
  redisdata:

networks:
  cafe-net:
    driver: bridge
